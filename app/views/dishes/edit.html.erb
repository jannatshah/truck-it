<!-- edit.html.erb -->
<div class = "container">
  <div class = "row justify-content-center">
    <div class="col-12 col-sm-6 pt-5">
    <h1>Update your dish info for <%= @truck.name %> here</h1>

    <%= render 'form', dish: @dish %>
    <p class="text-center"> <%= link_to 'Back', truck_path(@truck) %></p>
  </div>
</div>
